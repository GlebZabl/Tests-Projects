    Проект состоит из 2х кусков: первый http сервер, слушающий запросы следующего вида
"someip:8000/sendMessage/some_text", где someip - айпи машины на которой он развёрнут,
а some_text - текст отправляемого сообщения (например чтоб отправить "hi", при условии что сервер
развёрнут на том же компьютере с которого делаем запрос, в адресную строку необходимо ввести
"127.0.0.1:8000/sendMessage/hi"). Получив запрос сервер передаёт сообщение в менеджер очередей,
и отвечает клиенту удачно ли всё прошло(если менеджер очередей отвалился то на клиент вернётся сообщение
о том, что сервер временно недоступен и 502 ошибка)
    Вторая часть проекта это обработчик, он получает сообщения из менеджера очередей и записывает их в 
базу данных, либо в случае если база данных не отвечает кладёт обратно в менеджер очередей. Если соединение с
менеджером очередей по какой то причине упало, оно поднимается заново.
    В папках находятся конфигурационны файлы, следующего вида: для http-servers первая строчка адрес подключения 
к менеджеру очередей, вторая строчка название очереди. для listener-а первая строчка адрес подключения 
к базе данных, вторая адрес подключения к менеджеру очередей, третья название очереди.
    Для корректной работы необходимо удостовериться что имя очереди одинаково в обоих конфигурационных файлах
и соответствует имени очереди в менеджере очередей.
